{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "description": "A backend for a course application (FSO2019)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:server": "nodemon -L server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../phonebook && npm run build --prod && cp -r build ../phonebook_backend/",
    "lint": "eslint ."
  },
  "author": "Berkay SENKOYLU",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^6.5.1",
    "nodemon": "^1.19.3"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "debug": "^4.1.1",
    "dotenv": "^8.1.0",
    "express": "^4.17.1",
    "mongoose": "^5.7.4",
    "mongoose-unique-validator": "^2.0.3",
    "morgan": "^1.9.1"
  },
  "engines": {
    "node": "11.12.0"
  }
}
